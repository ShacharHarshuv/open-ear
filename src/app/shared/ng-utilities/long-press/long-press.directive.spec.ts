import { LongPressDirective } from './long-press.directive';

// todo: add some sort of tests?
describe('LongPressDirective', () => {
  it('should create an instance', () => {
    // const directive = new LongPressDirective();
    // expect(directive).toBeTruthy();
  });
});
